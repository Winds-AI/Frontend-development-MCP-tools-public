node-modules
**node_modules**
**/node_modules

# Environment files (ignore at any depth)
.env
.env.*
*.env
**/.env
**/.env.*
**/*.env

# Project config with secrets
chrome-extension/projects.json
**/projects.json

# Vectra semantic index (per-project indices)
.vectra/
**/.vectra/

# Local backups created by setup UI (should never be committed)
.afbt/
**/.afbt/